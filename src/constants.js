
const WIDTH = window.innerWidth;
const HEIGHT = window.innerHeight;

const colors = ['red', 'violet', 'yellow', 'green', 'blue'];

const n = 80; // population size
const randomRate = 10;
const radius = 20; // radius of the atoms
const detectionDistance = 100;